# Copy this block into ~/.aws/credentials (replace PASTE_* values from IAM).
# Your ingestion/.env should have: AWS_PROFILE=market-trend-exp-bronze-writer
#
# Do NOT commit real keys to git.

[market-trend-exp-bronze-writer]
aws_access_key_id = PASTE_ACCESS_KEY_ID_HERE
aws_secret_access_key = PASTE_SECRET_ACCESS_KEY_HERE
