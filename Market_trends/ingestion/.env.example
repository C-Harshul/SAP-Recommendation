EG_ENV=dev
EG_S3_BUCKET=market-trend-exp2
AWS_REGION=us-east-1
# Pick which AWS identity to use (profile name in ~/.aws/credentials)
# AWS_PROFILE=eg-market-trends
# Or explicit keys (do not commit):
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# Real AWS only — do NOT set AWS_ENDPOINT_URL for LocalStack
KAGGLE_USERNAME=
KAGGLE_KEY=
