# recommendation_engine/tools/requirements.txt
# Dependencies for the interview conversion tooling.
# Kept separate from the main recommendation_engine requirements
# so the GitHub Actions runner only installs what it needs.

python-docx==1.1.2
boto3==1.34.69

# Local web UI (tools/server.py) for converting transcripts via a browser.
# Version floors kept in sync with the main engine's pyproject.toml.
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9